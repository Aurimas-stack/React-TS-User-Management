export enum Routes {
  Login = '/login',
  Users = '/items',
  Weak = '/items/weak',
  Reused = '/items/reused',
  Root = '/',
  Old = '/items/old',
}

export enum API {
  Login = 'api/login',
  Items = 'api/items',
  User = 'api/user',
}

export const MS_PER_DAY = 1000 * 60 * 60 * 24;